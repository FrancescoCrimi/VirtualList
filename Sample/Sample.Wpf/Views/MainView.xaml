<Window x:Class="CiccioSoft.VirtualList.Sample.Wpf.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CiccioSoft.VirtualList.Sample.Wpf.Views"
        xmlns:viewmodels="clr-namespace:CiccioSoft.VirtualList.Sample.Wpf.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Title="{Binding Content.Title, ElementName=shellFrame}"
        Height="350"
        Width="350">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Menu>
            <MenuItem Header="ListView"
                      Command="{Binding OpenListViewCommand, Mode=OneWay}" />
            <MenuItem Header="DataGrid"
                      Command="{Binding OpenDataGridCommand, Mode=OneWay}" />
        </Menu>

        <Frame Grid.Row="1"
               x:Name="shellFrame"
               NavigationUIVisibility="Hidden"
               Source="/Views/ListViewView.xaml" />

    </Grid>

</Window>
